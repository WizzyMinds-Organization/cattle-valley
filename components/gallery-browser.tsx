'use client';
import { useEffect, useMemo, useState } from 'react';
import Link from 'next/link';
import { FileText } from 'lucide-react';
import { localCmsStorageKey } from '@/lib/local-cms';
type Item={id:string;title:string;image?:string;category?:string;tags?:string[];slug?:string;createdAt?:string;status?:string;url?:string;detail?:string};
const fallback=[{id:'g1',title:'Healthy herd',category:'Latest',slug:'latest',image:'https://images.unsplash.com/photo-1523742810063-7de5c3c16809?auto=format&fit=crop&w=900&q=80',createdAt:new Date().toISOString(),status:'Published'}];
export function GalleryBrowser({documentsOnly=false}:{documentsOnly?:boolean}){const[gallery,setGallery]=useState<Item[]>(fallback);const[docs,setDocs]=useState<Item[]>([]);const[active,setActive]=useState('latest');useEffect(()=>{try{const data=JSON.parse(localStorage.getItem(localCmsStorageKey)||'{}');if(Array.isArray(data.Gallery))setGallery(data.Gallery);if(Array.isArray(data.Documents))setDocs(data.Documents);const shared=new URLSearchParams(window.location.search).get('category');if(shared)setActive(shared)}catch{}},[]);const categories=useMemo(()=>['latest',...Array.from(new Set(gallery.flatMap(item=>item.tags?.length?item.tags:[item.slug||'general'])))], [gallery]);const images=gallery.filter(item=>{if(item.status==='Draft')return false;if(active==='latest'){const d=new Date(item.createdAt||0);return Date.now()-d.getTime()<=31*24*60*60*1000}return (item.tags?.length?item.tags:[item.slug||'general']).includes(active)});const allDocs=docs.filter(doc=>doc.status!=='Draft').sort((a,b)=>new Date(b.createdAt||0).getTime()-new Date(a.createdAt||0).getTime());const visibleDocs=documentsOnly?allDocs:allDocs.slice(0,10);function choose(category:string){setActive(category);const url=new URL(window.location.href);if(category==='latest')url.searchParams.delete('category');else url.searchParams.set('category',category);window.history.replaceState({},'',url)}return <>{!documentsOnly&&<><div className="gallery-filters" aria-label="Gallery categories">{categories.map(category=><button onClick={()=>choose(category)} className={active===category?'is-active':''} key={category}>{category==='latest'?'Latest':category.replace(/-/g,' ')}</button>)}</div><div className="masonry">{images.map(image=><img key={image.id} src={image.image} alt={image.title}/>)}</div>{images.length===0&&<p className="gallery-empty">No images in this category yet.</p>}</>}<section className="documents-preview"><span className="eyebrow">Document library</span><h2 className="display split-heading">{documentsOnly?'All documents.':'Useful resources.'}</h2><div className="documents-list">{visibleDocs.map(doc=><a className="document-row" key={doc.id} href={doc.url||'#'} download={doc.title}><FileText size={19}/><span><b>{doc.title}</b><small>{doc.detail}</small></span></a>)}{visibleDocs.length===0&&<p className="gallery-empty">No documents have been uploaded yet.</p>}</div>{!documentsOnly&&<Link href="/documents" className="button light">More documents</Link>}</section></>}
